# Optionale Handschrift-OCR ohne CUDA
# Fuer Debian-LXC/CPU. PyTorch wird bewusst aus dem CPU-Index installiert.
--extra-index-url https://download.pytorch.org/whl/cpu
transformers>=4.40.0
torch>=2.2.0+cpu
